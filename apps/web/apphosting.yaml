runConfig:
  minInstances: 0
  maxInstances: 5
  concurrency: 80
  cpu: 1
  memoryMiB: 512

# Environment values are configured after backend creation because the generated URL
# is not known before the first rollout. Secrets must use Cloud Secret Manager.
