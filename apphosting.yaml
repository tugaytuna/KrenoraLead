runConfig:
  minInstances: 0
  maxInstances: 5
  concurrency: 80
  cpu: 1
  memoryMiB: 512

# Environment values are configured in App Hosting or Secret Manager.
# Keep service credentials out of NEXT_PUBLIC_ variables.
